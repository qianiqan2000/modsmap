services:
  modsmap:
    build: .
    container_name: modsmap-app
    ports:
      - "80:3000"
    restart: always
    environment:
      - PORT=3000
      - VITE_SUPABASE_URL=${VITE_SUPABASE_URL}
      - VITE_SUPABASE_ANON_KEY=${VITE_SUPABASE_ANON_KEY}
